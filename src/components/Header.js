import React, {Component} from 'react';
import Logout from './Logout';

export class Header extends Component {

  render(){
    return(
      <header>
        <Logout />   
      </header>
    )
  }
}